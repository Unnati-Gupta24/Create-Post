<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-black text-center">
        <a class="font-mono text-xl mb-10 font-semibold text-red-700" href="/logout">Logout</a>
        <h1 class="text-white font-mono text-4xl mt-5 mb-5 font-bold">Welcome, <%= user.name %></h1>
        <h3 class="text-white font-mono text-2xl mb-5 font-semibold">You can create a post</h3>
        <form class="flex flex-col items-center justify-center" action="/post" method="post">
            <textarea placeholder="  What's on your Mind?..." class="text-white mb-5 font-mono w-1/3 rounded-lg bg-transparent resize border-4 border-zinc-700" name="content"></textarea>
            <input class="block rounded-lg outline-none p-2 text-white font-semibold bg-zinc-700" type="submit" value="Create Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400 m-5">Your Posts.</h3>
            <div class="flex flex-col items-center justify-center postcontainer text-white">
                <% user.posts.forEach(function(post){ %>
                <div class="post w-1/3 p-5 border-2 border-zinc-700 bg-zinc-800 rounded-lg mb-5">
                    <h4>@someone</h4>
                    <p>123...</p>
                    <div class="btns flex items-center justify-center mt-2">
                        <a class="text-blue-500" href="">Like</a>
                    </div>
                </div>
            </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>